import { Home } from '@/components/home';
import { getContent,getCategories,getStats,getSettings,getCreators } from '@/lib/data';
export const revalidate = 60;
export default async function Page(){const [items,categories,stats,homepage,season,faq,creators]=await Promise.all([getContent(),getCategories(),getStats(),getSettings('homepage'),getSettings('season'),getSettings('faq'),getCreators()]);return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({'@context':'https://schema.org','@type':'WebSite',name:'HOMELAND',url:process.env.NEXT_PUBLIC_SITE_URL||'https://homeland.example',description:'A premium digital content platform for creators and communities.',potentialAction:{'@type':'SearchAction',target:{'@type':'EntryPoint',urlTemplate:(process.env.NEXT_PUBLIC_SITE_URL||'https://homeland.example')+'/search?q={search_term_string}'},'query-input':'required name=search_term_string'}}).replace(/</g,'\\u003c')}}/><Home items={items} categories={categories} stats={stats} homepage={homepage} season={season} faq={(faq.items||[]) as {q:string;a:string}[]} creators={creators}/></>}
